apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "python-apps.fullname" . }}-test-connection"
  labels:
    {{- include "python-apps.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "python-apps.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
